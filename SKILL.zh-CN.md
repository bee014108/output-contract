---
name: output-contract
description: 产出文件归置契约。创建或修改代码、运行分析、生成日志或交付文件，以及把暂存成果归入项目时使用；开工前声明唯一维护位置和落点，执行中说明例外，收尾核对散件与副本。用户调用 /output-contract 时也使用；纯只读任务不需要。
---

# Output Contract（产出归置契约）

> 本文件是英文 `SKILL.md` 的中文版。中文用户可把本文件重命名为 `SKILL.md` 后安装。

## 核心原则

**先说明放哪，再落盘；每份成果只有一个持续维护的位置；偏离约定必须告诉用户。**
契约要轻：归属明确时，一条消息告知后直接执行；仅在归属存在实质歧义时先澄清。告知不代替必要的写入权限或破坏性操作授权。
本契约管理本任务创建、修改及触发工具生成的文件，不把聊天应用自身保存的记录当成项目产物。

## 开工流程

1. **判归属并检查现状**：优先用户指定路径，其次当前任务明确关联的已有项目或工作台；读取已有目录约定，检查目标是否已有成果，避免另起一套。
   - 已有项目：代码在原位置修改，派生产物沿用现有 build、CLI 或分析目录结构。`scripts/` 只作为新建独立脚本的默认位置。
   - 新的独立工作：默认提议 `<工作区根>/{topic}_{YYYYMMDD}/`（工作区根＝本环境存放项目文件夹的默认位置），先声明并检查可用性。与用户指定位置或当前任务约定冲突时，以已有约定为准。
   - 归属暂不明确但可以独立探索：声明一个暂存位置并标记"待归属"；实质影响项目选择的歧义先澄清。已有项目成果不默认暂存到聊天目录。
2. **报契约**：用一条消息说明唯一维护位置、代码／日志／交付物落点，以及必要的暂存或目录外位置。例如：
   > 本次产出放在 `<项目路径>`，沿用现有结构；代码原位维护，运行日志和结果放 `<运行目录>`。这里是唯一维护位置，聊天中直接链接交付；额外暂存会说明位置和原因。
3. **记录并开工**：多文件或持续迭代任务在契约根维护 `_index.md`；已有同等用途清单则复用。仅创建实际需要的目录。
   - 修改少量既有文件，或预估不超过两个文件的一次性交付／探查，可只在开工与交付消息记录，不为满足契约额外创建索引或日志。
   - 简化的是记录手续，事先声明位置、唯一维护位置和例外说明仍然适用。发展为多文件或持续迭代任务时补清单。
4. **按约定写入**：执行中记录实际写入路径；新增产物类别或更换落点时，先更新声明及已有清单。

## 唯一维护位置与版本

- 同一用途、同一格式的成果只有一个主位置，后续修改只发生在那里。聊天目录默认通过链接引用项目成果，不另存一套持续维护的副本。
- 代码默认原位维护，优先由已有 Git 保存历史；不因每次修改自动生成 `01/02/final/final2`。确需保留独立版本或无版本控制时的重要快照，说明用途、基于哪个版本，以及当前主版本的位置；不自动提交 Git。
- 需要留存的实验运行结果按运行标识分目录，例如 `runs/20260925_153000/`，并记录对应代码提交或必要的代码／配置快照。可重建产物是否覆盖遵循项目约定；需保留的结果使用新运行目录。
- 同一成果的 SVG、PDF、PNG 等不同格式可以并存。明确用途的发布包、交付副本或备份也可以保留，但要标明来源、用途和主位置，不把副本当作第二个编辑入口。

## 路径与日志

- 输出路径集中定义，从声明的根目录派生，并在运行时解析为绝对路径。根目录可以来自项目配置、脚本位置或 `--output-root`，无需把机器路径写死在每个脚本里。
- 相对路径参数以明确的项目根或配置文件目录为基准；最终落点不随启动时的 cwd 漂移。向 `open`、`savefig`、`to_csv` 等传入已解析的目标路径。
- 独立新工作可采用 `scripts/`、`logs/`、`outputs/`、`scratch/`；既有项目优先遵循自身结构。暂存不等于已授权删除。
- 长时间运行、批处理及需要复现的分析保存运行日志到约定位置，记录运行标识、命令、关键配置和退出状态。捕获标准输出／错误时保留被执行程序的失败状态，不把日志管道成功当成程序成功；短小编辑或只读检查不强制额外生成日志文件。

## 目录外文件与例外

- 约定目录不可写、磁盘不可用或缺少权限时，说明实际限制并按环境要求处理权限；不静默改存聊天目录。改变落点必须明确更新契约，无法继续时报告未完成项。
- 可配置的缓存、预览和临时文件尽量纳入约定暂存位置。工具强制生成在其他位置的文件，记录实际路径或已知范围、原因、用途及是否需后续处理；预先已知则在开工时说明，意外发现则及时说明。
- 目录外文件仅写进 `_index.md` 不算完成告知；交付消息必须汇总其位置、原因和处理状态。无法获知工具内部文件位置时，如实说明核对范围和限制。

## 暂存转正与重复副本

将暂存成果归入项目时，完成以下交接；"已复制"不等于"已整理完成"：

1. 明确项目内的目标位置和转正后的唯一主版本。目标已存在时先比较，不能直接覆盖不同内容；内容相同也不能单凭哈希判定某份既有文件可删除。
2. 核对迁移内容，更新受影响的路径和引用，并做与变更相称的验证。复制文件核对内容；移动脚本或项目文件还需检查必要的导入、资源及运行路径。
3. 后续修改只针对主位置。旧位置按授权移除，或保留为明确标记的暂存／交付副本；必要时用路径说明代替完整成果副本。
4. 告诉用户主位置、旧位置剩余内容、保留原因和待处理事项。因权限或已有文件保护未能处理的副本明确标为"待处理暂存副本"。

仅移动本任务新建且已确认归属的文件，并在移动后验证引用；移动历史文件须先报清单取得授权。删除文件遵循用户授权，不因文件在 `scratch/` 或已被复制就自动删除。

## _index.md 模板

索引只记录归置；沿用已有清单时按同等信息更新，不另外复制清单。大量运行文件可按运行目录分组记录。

```markdown
# <任务一句话>
归属项目：…
唯一维护位置：…
约定落点：代码 …；日志 …；交付物 …；暂存 …

## 产物清单
| 路径 | 用途 | 角色及状态 |
|---|---|---|
| scripts/analyze.py | 分析入口 | 主版本／待验证 |
| runs/<运行标识>/ | 本次配置、日志和结果 | 运行留档 |

## 例外与副本
| 实际路径 | 原因及用途 | 主版本位置 | 处理状态 |
|---|---|---|---|

## 收尾核对
已核对范围：…
未归位文件、待处理副本及原因：无／…
```

## 收尾核对

1. 根据执行记录和产物清单核对实际写入路径，检查本任务涉及的聊天目录、暂存目录、项目落点及已知工具输出位置。时间戳仅作为线索，不据此认领、移动或删除其他任务的文件；不为清点而全盘扫描。
2. 核对主要产物是否存在、主版本是否明确、例外是否已说明，以及迁移后的必要验证是否完成。未完成的归位、副本处理或核对保留为明确的未完成项。
3. 更新已有索引，在交付消息中列出主要产物链接，并报告目录外文件及重复副本状态。只有在已核对范围内确认没有例外时才写"目录外文件：无；重复副本：无"；检查受限则说明范围，不能宣称全局无散件。

## 边界

- 原始数据保持原位置，本契约只管理派生产物；不因归置要求复制或移动原始数据。按数据角色判断，不把整个盘符视为原始数据目录。
- 项目的工程化体系和已有源代码布局优先；本契约只补充未覆盖的产物、日志和暂存位置。
- 项目文档、实验记录、记忆系统遵循各自规则和授权。`_index.md` 不重复记录推理或实验事实；本契约不额外触发这些日志或记忆的写入。
- 存量散件不自动整理，不扩大为全项目清理；仅处理本次授权范围内的文件。
